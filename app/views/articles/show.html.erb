<nav class="navbar d-flex navbar-light bg-light p-4">
  <h3 display-3>Wikinimous</h3>

  <div class="d-flex">
    <a class="btn btn-primary" href="/articles/new">New Article</a>
  </div>
</nav>

<div class="container mt-4">
  <div class="d-flex justify-content-between">
    <h1 class="fw-normal display-4 pb-3 mr-3"><%= @article.title %><h1>
    <%= link_to raw('<i class="fa-solid fa-pen"></i>'), edit_article_path %>
    <%= link_to raw('<i class="fa-solid fa-trash"></i>'), article_path(@article.id), data: { turbo_method: :delete, turbo_confirmation: "Estas seguro/a que quieres borrar este registro?" } %>
  </div>
  <hr>
  <blockquote class="blockquote">
    <p><%= @article.content %></p>
  </blockquote>
</div>
